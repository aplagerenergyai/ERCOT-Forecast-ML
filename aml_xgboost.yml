$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command

display_name: train_xgboost
code: .
command: >
  python train_xgboost.py --data_path ./inputs/features/features.parquet

environment: azureml:forecast-env@latest
compute: cpu-cluster

inputs:
  features:
    type: uri_file
    path: azureml://datastores/workspaceblobstore/paths/features/features.parquet

outputs:
  model_output:
    mode: rw_mount

